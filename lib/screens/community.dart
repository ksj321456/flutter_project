import 'package:flutter/material.dart';

class CommunityP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('커뮤니티'),
      ),
      body: Center(
        child: Text(''),
      ),
    );
  }
}